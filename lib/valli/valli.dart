import 'package:flutter/material.dart';

class ValliPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text('Valli: hello world'),
      );
  }
}
