import 'package:flutter/material.dart';

class FuugaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text('Fuuga: Hello'),
      );
  }
}
